name: Deploy Front-end
on:
  push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: docker build -t sbosse/react-test -f Dockerfile.dev .
      - run: docker run -e CI=true sbosse/react-test npm test
